# RPC endpoint for the Sepolia testnet (e.g. an Alchemy or Infura URL with your API key)
SEPOLIA_RPC_URL=

# Private key of the deployer account (Sepolia test ETH only — never commit a real key)
PRIVATE_KEY=
